# Skill Backlog

## 2026-05-13

- `masked-imagegen-protection`
  - 场景：用户需要长期重复做同一 IP 或吉祥物的人物替换，希望先固定男版 / 女版角色设定，再映射到不同照片人物上。
  - 价值：把“角色参考包 + 局部替换”的前置准备标准化，减少每次临时重建人设导致的风格漂移和返工。
  - 建议沉淀：补一份“角色参考包清单”，至少覆盖正脸半身、3/4 半身、全身站姿、手部或侧脸局部，以及固定字段如发型、服装、主色、鞋子、表情、材质。

## 2026-05-04

- `masked-imagegen-protection`
  - 场景：用户希望先人工修保护区，再用内置 `image_gen` 做局部生成，最后把原图保护区无损盖回去。
  - 价值：把“软约束生成 + 硬回盖恢复”的流程固定下来，减少每次重新解释 built-in `image_gen` 遮罩限制和后处理步骤的返工。
  - 新增沉淀：当 raw 图局部几何偏移明显时，补一层“按灰区联通块手动对齐后再合并”的流程，避免灰区边缘对不齐。
  - 新增沉淀：当用户明确拒绝画遮罩、但仍要求“只改天空和整体色调、不要改建筑等固定主体”时，需要单独使用更强约束的“无遮罩直改天空模板”，不能复用宽泛的整图氛围重绘提示词。
  - 新增沉淀：当用户要画灰色区时，skill 默认应一次性给出 3 条命令，分别是“画遮罩”“联通块手动对齐”“读取 `*-region-transforms.json` 后回盖”，避免只给后两条造成流程断档。
  - 新增沉淀：联通块手动对齐前，应先自动在上下左右各 20 像素内做双重循环搜索，按“区域像素差的和最小”选出初始位移，并顺手输出一版自动回盖图；不够准时再手动微调。
  - 位置：`D:\workspace\P图\.codex\skills\masked-imagegen-protection\`
