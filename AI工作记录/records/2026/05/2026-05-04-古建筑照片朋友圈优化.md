# 古建筑照片朋友圈优化

- 日期：2026-05-04
- 来源：AI 对话摘要
- 类型：记录
- 相关目录：D:\workspace\P图
- 相关 skill：record-and-reflect-review, imagegen
- 标签：修图, 古建筑, 朋友圈, 图片优化

## 背景

用户希望把当前目录下的 3 张古建筑照片优化得更适合发朋友圈，同时明确要求不能修改建筑本身内容，避免画面中的文字或建筑结构被替换。

## 关键过程

- 检查当前工作目录，确认有 `1.jpg`、`2.jpg`、`3.jpg` 三张图片。
- 读取项目规则与 `$record-and-reflect-review`、`imagegen` skill，按要求在项目目录内维护摘要记录，并使用内置图像编辑流程。
- 查看三张原图，确认主要问题为阴天导致的灰、闷、反差不足，以及部分画面留白较多。
- 确定修图策略仅限于曝光、对比、色彩、清晰度、氛围和轻度构图优化，不改动建筑主体、牌匾、对联和原有文字内容。
- 试过一轮生成式编辑预览，但判断其对天空和局部细节的改动风险过高，不适合“建筑和文字不能变”的要求。
- 改用本地确定性调色流程，对原图做阴影提亮、高光压制、轻微对比增强、适度饱和度提升和克制的锐化处理。

## 结果

- 已完成素材盘点与约束确认。
- 当前目录不是 Git 仓库，因此本次不涉及提交与推送。
- 已输出 3 张朋友圈优化版图片到 `D:\workspace\P图\朋友圈优化\`：
  - `1-朋友圈版.jpg`
  - `2-朋友圈版.jpg`
  - `3-朋友圈版.jpg`
- 已校验输出尺寸与原图一致：
  - `1.jpg`：`1706x1279` -> `1-朋友圈版.jpg`：`1706x1279`
  - `2.jpg`：`1279x1706` -> `2-朋友圈版.jpg`：`1279x1706`
  - `3.jpg`：`1700x1280` -> `3-朋友圈版.jpg`：`1700x1280`

## 可复用经验

- 对古建筑和带文字场景做朋友圈优化时，应优先采用全局调色和轻度构图，避免生成式替换导致牌匾文字、对联或屋檐纹样失真。
- 当用户明确要求“不能改建筑和文字”时，优先选择确定性像素级调色，而不是生成式重绘。

## Skill 观察

- 是否出现新 skill 候选：暂未确定。
- 是否应该优化已有 skill：可考虑补充“本地照片安全修图”的工作流说明。
- 触发条件或典型用户说法：帮我把这几张图 P 一下，但是不能改建筑和文字。

## 后续事项

- [x] 生成并保存 3 张适合朋友圈发布的优化版本。
- [x] 复核输出，确认建筑本体和文字未被替换。

## 追加沟通

- 用户随后澄清：需要“生成式替换”，但建筑本身内容不允许改动；允许对建筑整体做统一调色，不允许改变建筑结构、牌匾、对联、纹样和文字。
- 下一步方案不再采用整图重绘思路，而是优先考虑“建筑外区域替换 + 建筑区域锁定 + 全局色调统一”的编辑策略，并先向用户说明方案再执行。

## 样例进展

- 已对 `1.jpg` 制作一版生成式替换样例，策略为：单独生成天空素材，再以本地遮罩只替换建筑外天空区域，避免建筑本体被模型重绘。
- 在多轮遮罩调整后，保留了一个更稳的“裁切版”样例，用小幅横向裁切去掉左右边缘的过渡区，降低建筑边缘失真风险。
- 当前样例输出目录：`D:\workspace\P图\生成式替换样例\`
  - `1-纯天空生成参考.png`
  - `1-天空遮罩预览.png`
  - `1-生成式替换样例.jpg`
  - `1-生成式替换样例-裁切版.jpg`

## 遮罩工具

- 用户指出天空遮罩仍不够准确，因此新增本地手工修遮罩工具 `D:\workspace\P图\tools\manual_mask_editor.py`。
- 工具能力：加载原图、自动生成一版初始遮罩、支持鼠标左键补保护区、右键擦除保护区，并可在有替换参考图时实时导出合成预览。
- 已用 `python .\\tools\\manual_mask_editor.py --help` 与 `python -m py_compile .\\tools\\manual_mask_editor.py` 验证参数解析和语法正常；未做完整交互式人工测试。
- 后续按用户修正方案调整为同时导出 API 可用遮罩 PNG：透明区域表示允许模型修改，非透明区域表示保护原图。
- 根据用户进一步要求，工具已改成“通用保护区模式”，不再把语义绑定为建筑或天空。

## 方案修正

- 用户进一步澄清，正确流程应为：
  1. 先生成一版初始遮罩；
  2. 用户人工调整遮罩；
  3. 使用“原图 + 遮罩”约束模型生成，指定遮罩保护区不能修改；
  4. 生成后再把原图建筑区域覆盖回结果图；
  5. 最后根据边缘和氛围决定是否微调参数。
- 这意味着当前“先生成天空，再本地直接合成”的路径不作为最终方案，只保留为过程性试验。

## 生成流程准备

- 已定位用户人工修正后的遮罩文件：
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-mask.png`
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-api-mask.png`
- 已安装 `openai` Python 包，并对 fallback CLI 的 `edit --mask` 参数做了 dry-run 验证，确认原图、遮罩、prompt 和输出路径匹配。
- 当前真实调用的唯一阻塞是当前会话未设置 `OPENAI_API_KEY`，因此暂时只能 dry-run，不能发起实际 Image API 编辑请求。
- 为后续最后一步补充了保护区回盖脚本：`D:\workspace\P图\tools\apply_protection_overlay.py`，并完成语法与 `--help` 校验。

## 方案调整

- 用户说明当前没有 `OPENAI_API_KEY`，因此不再继续 CLI / API `edit --mask` 路线。
- 随后转而确认“内置 `image_gen` 是否支持传遮罩图”。
- 基于当前 Codex 会话里的内置 `image_gen` 工具定义和本地 `imagegen` skill 说明，结论为：
  - 底层 Images API 支持显式 `mask`；
  - 但当前内置 `image_gen` 工具本身不提供 `mask` 参数入口，不能直接把本地遮罩图传进去做精确受控编辑。

## Built-in 遮罩参考图可行性

- 用户进一步提出：不把遮罩当参数，而是把 `mask.png` 当参考图一并喂给内置 `image_gen`，希望模型只修改遮罩区、其余区域保持不变。
- 当前判断：
  - 这种方式可以作为“软约束提示”尝试；
  - 但它不等价于 API 级硬遮罩，不能保证未遮罩区域绝对不变；
  - built-in `image_gen` 更适合“基于上下文尽量保留”的编辑，不适合需要像素级锁定人物、文字、logo、边缘和纹理的场景。

## 项目 Skill 沉淀

- 用户要求把当前流程整合成一个可复用 skill。
- 已在项目内新增本地 skill：`D:\workspace\P图\.codex\skills\masked-imagegen-protection\`
  - `SKILL.md`
  - `references\built-in-prompt-template.md`
- 随后按用户要求继续调整：
  - `SKILL.md` 与提示词模板已改为中文。
  - 脚本已从 `D:\workspace\P图\tools\` 移入 skill 目录：
    - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py`
    - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`
  - 两个脚本的命令行帮助和主要交互文案也已同步中文化。
- 用户进一步要求把 skill 放到 `.codex` 目录下以便自动识别，因此 skill 已从工作区普通 `skills\` 目录移动到 `.codex\skills\`。
- 新增 `D:\workspace\P图\AI工作记录\skill-backlog.md`，记录该工作流已沉淀为项目 skill 候选并已落地。

## Skill 校验

- 使用全局 `skill-creator` 的校验脚本执行：
  - `python C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py D:\workspace\P图\.codex\skills\masked-imagegen-protection`
- 首次校验失败，原因不是 skill 结构问题，而是 Windows 默认编码按 `gbk` 读取 `SKILL.md` 时遇到非 ASCII 字符。
- 之后先临时改成纯英文结构完成一次校验，再在保留中文内容的前提下使用 `PYTHONUTF8=1` 重新校验。
- 最终校验结果：`Skill is valid!`
- 补充校验：
  - `python -m py_compile D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`
  - 两个脚本语法校验通过。

## 基于 Skill 的首轮实跑

- 用户确认开始按新 skill 处理 `1.jpg`。
- 使用文件：
  - 原图：`D:\workspace\P图\1.jpg`
  - 用户修正后的遮罩：
    - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-mask.png`
    - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-api-mask.png`
- 已用 `view_image` 将原图和可见黑白遮罩图加载进对话上下文，再调用 built-in `image_gen` 做第一轮生成。
- 本轮生成意图：
  - 只修改黑色可编辑区，即建筑外部天空和未受保护的外部背景氛围；
  - 目标为真实、自然、适合朋友圈照片的暖色傍晚天空；
  - 明确要求不改建筑结构、灯笼、牌匾、对联、文字、栏杆、帘幕和保护区边界。
- built-in 生成图实际落盘位置：
  - `C:\Users\Administrator\.codex\generated_images\019df19d-1aa2-7460-b74b-b9134b0e241e\ig_08e94050560209260169f852104fcc8191a2dbb1ff9ddcf557.png`
- 已复制 raw 结果到工作区：
  - `D:\workspace\P图\assets\1-edited-v1-raw.png`
- 已执行保护区回盖脚本，输出最终结果：
  - `D:\workspace\P图\assets\1-edited-v1.png`

## 本轮结果观察

- raw 生成图仍然对建筑区域做了轻度重绘，这再次验证 built-in `image_gen` 的遮罩参考图只能提供软约束。
- 回盖后的最终图保住了建筑主体、文字和大部分结构细节，同时把天空替换成了暖色晚霞。
- 当前结果可作为第一版样例交付。
- 残余风险：
  - 屋檐边界附近仍可能存在轻微融合痕迹；
  - 如果用户希望天空更克制、更接近真实拍摄感，可能需要第二轮把晚霞强度和整体暖色再收一点。

## 光影融合修正

- 用户指出：直接把原图保护区硬覆盖回去后，保护区与新天空之间的光影关系仍不够自然。
- 已按该思路增强 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`：
  - 新增 `--match-tones`
    - 用 raw 生成图在保护区内的统计结果，反推原图保护区需要做的亮度和色偏匹配；
  - 新增 `--match-erode`
    - 先向内收缩保护区再做统计，减少边缘污染；
  - 新增 `--feather`
    - 对保护区 alpha 轻微羽化，减轻硬边界。
- 已对 `1.jpg` 当前 raw 生成图重跑一版：
  - 输出：`D:\workspace\P图\assets\1-edited-v2-tone-matched.png`
  - 使用参数：`--match-tones --match-erode 4 --feather 1.8`
  - 本次匹配统计：
    - `matched_pixels=1117807`
    - `l_scale=0.8816`
    - `l_shift=-2.4077`
    - `a_shift=0.3253`
    - `b_shift=1.4047`
- 这版的目标不是改结构，而是让保护区覆盖回去以后，在亮度、反差和轻微色温上更接近生成图给出的环境光关系。

## 下一步方案判断

- 用户进一步追问：是否能在生成阶段就“不修改遮盖部分内容”，但又让这些区域与新天空保持更自然的光影关系。
- 当前判断：
  - 如果“不能修改”指的是像素完全不变，那么不能同时获得新的光影关系，因为光影变化本身就是像素变化。
  - 更可行的定义应当改成：
    - 结构和语义内容不变；
    - 但允许保护区中的部分区域只发生轻微的亮度、对比度、色温和阴影色偏变化。
- 因此更优流程不是单一保护区，而是把保护区拆成两层：
  - 但用户随后进一步判断，不再需要“白色硬保护区”；
  - 最终改成两分区：
    - 黑区：可编辑区；
    - 灰区：联动区，只允许光影联动，不允许改结构和文字。
- 这样生成阶段可以尝试保留灰区的环境光变化，后处理阶段只对灰区做受控混合，而不是把整片建筑完全盖回去。

## 两分区模式落地

- 已把项目 skill 和脚本语义从“白黑保护区”改成“灰黑两分区”：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py`
    - 初始遮罩改为灰色联动区 + 黑色可编辑区；
    - 左键为“添加灰色联动区”，右键为“擦除到黑色可编辑区”。
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`
    - 色调匹配不再只认纯白区域；
    - 新增 `--match-threshold`，允许灰区参与统计；
    - 回盖语义改成“联动区混合回去”，不再等同于纯硬覆盖。
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\SKILL.md`
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\references\built-in-prompt-template.md`
    - 均已同步改成“灰区只联动光影，黑区可编辑”的说明。
- 校验：
  - `quick_validate.py` 通过；
  - `py_compile` 通过。

## 1.jpg 灰黑版重跑

- 为了不让用户重画，已把现有白黑遮罩直接转换成灰黑版：
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-gray-black-mask.png`
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-gray-black-api-mask.png`
- 使用该灰黑遮罩重新调用 built-in `image_gen`。
- 本轮 raw 生成图：
  - `D:\workspace\P图\assets\1-edited-v3-gray-mask-raw.png`
- 本轮联动区混合后的结果：
  - `D:\workspace\P图\assets\1-edited-v3-gray-mask.png`
- 后处理参数：
  - `--match-tones`
  - `--match-threshold 32`
  - `--match-erode 4`
  - `--feather 1.2`
- 本轮色调匹配统计：
  - `matched_pixels=1125606`
  - `l_scale=0.7967`
  - `l_shift=-5.9377`
  - `a_shift=0.7168`
  - `b_shift=4.3358`
- 主观观察：
  - 相比整片建筑硬回盖的版本，这一版保留了更多与晚霞一致的环境光关系；
  - 建筑主体仍有一定结构稳定性，但因为 built-in `image_gen` 没有硬遮罩，灰区内仍可能出现局部细节被轻微重绘的风险。

## 从头重跑准备

- 用户要求从头重画遮罩，并完整重跑这条 skill 流程。
- 已按要求修改 skill 逻辑：
  - 联动区色调匹配改成“按联通区域分别匹配”，不再整片灰区共用一套参数。
  - 遮罩编辑器改成默认全黑空白画布，由用户直接手画灰区，不再自动生成初版遮罩。
- 已清理 `1.jpg` 之前的中间输出和旧灰黑派生遮罩。
- 已重新启动遮罩编辑器，等待用户保存新的灰黑遮罩后继续生成流程。

## 1.jpg 从头完整重跑

- 用户已重新手画并保存新遮罩：
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-mask.png`
  - `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-api-mask.png`
- 观察到这次灰区更克制，只保留了 3 块独立连续区域，适合按联通区域分别匹配。
- 已重新加载：
  - 原图 `D:\workspace\P图\1.jpg`
  - 新遮罩 `D:\workspace\P图\生成式替换样例\manual-mask\1-manual-mask.png`
- 已重新调用 built-in `image_gen` 生成 raw 图，并复制到工作区：
  - `D:\workspace\P图\assets\1-edited-v4-raw.png`
- 已用新的联通区域匹配脚本输出最终图：
  - `D:\workspace\P图\assets\1-edited-v4.png`
- 本轮后处理参数：
  - `--match-tones`
  - `--match-threshold 32`
  - `--match-erode 4`
  - `--feather 1.0`
- 本轮联通区域匹配统计：
  - `matched_pixels=45821`
  - `components=3`
  - component 1:
    - `area=20144`
    - `l_scale=1.3011`
    - `l_shift=-73.7457`
    - `a_shift=0.9848`
    - `b_shift=1.7838`
  - component 2:
    - `area=15988`
    - `l_scale=0.9187`
    - `l_shift=-20.1104`
    - `a_shift=4.8714`
    - `b_shift=6.838`
  - component 3:
    - `area=9689`
    - `l_scale=0.8244`
    - `l_shift=-19.3876`
    - `a_shift=0.842`
    - `b_shift=6.1178`
- 结果判断：
  - 这版已经按用户要求完成“重新画遮罩后从头完整跑 skill”；
  - 且后处理不再是整片灰区统一调色，而是按每个联通灰区分别做亮度、反差和色偏匹配。

## 重影问题修正

- 用户随后指出：替换后出现重影。
- 原因定位：
  - 不是只有生成图局部偏移；
  - 更核心的问题是旧脚本把灰区灰度值直接当作混合 alpha 使用；
  - 这会导致灰区内部持续按比例叠加“原图 + 生成图”，一旦生成图结构轻微漂移，就会出现双影。
- 已修正 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`：
  - 灰区内部改为 100% 使用“调色后的原图”；
  - 只在灰区边界根据阈值后的二值区域做羽化过渡；
  - 不再把灰度强度直接当作内部混合比例。
- 基于同一张 raw 图重新输出：
  - `D:\workspace\P图\assets\1-edited-v5-no-ghost.png`
- 这版的作用是单独验证“去除灰区内部重影”的补丁是否生效，不需要用户重画遮罩也不需要重跑生成。

## 生成阶段对齐限制

- 用户继续指出：即使回盖逻辑修正了，第 4 步调用 built-in `image_gen` 时本身仍可能发生局部几何偏移，导致边缘对不齐。
- 这里需要明确一个技术边界：
  - 当前 built-in `image_gen` 没有显式硬遮罩，也没有几何锁定能力；
  - 因此无法保证模型在生成阶段对建筑邻近区域做到像素级对齐。
- 结论：
  - 不能把“灰区的像素内容”继续寄希望于模型生成结果本身；
  - 更稳的流程应改成：
    1. built-in `image_gen` 只负责生成黑区里的天空/外部背景参考；
    2. 灰区不直接采用模型输出像素；
    3. 灰区只根据 raw 生成图推导环境光变化，再对原图做确定性的局部调光/调色；
    4. 最终成图中的灰区几何内容始终来自原图，而不是来自生成图。
- 这样可以从流程上规避“生成阶段局部偏移导致对不齐”的问题。

## 联通块手动对齐补偿

- 用户进一步提出：是否可以在最终合并前提供一个脚本，让用户手动微调几个原图灰区联通块的位置，对齐 raw 生成图后再开始调光调色和合并。
- 该方案可行，且已落地为 skill 内脚本：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\align_gray_regions.py`
- 新流程补充为：
  1. built-in `image_gen` 先出 raw 图；
  2. 如果灰区附近出现局部偏移，用对齐器按联通块微调原图灰区位置；
  3. 保存 `*-region-transforms.json`；
  4. 最终合并脚本读取该 JSON，先局部挪位，再按联通块分别做调光调色和回盖。
- 合并脚本已同步支持：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py`
  - 新参数：`--region-transforms`
- 这样灰区的几何内容仍然来自原图，但允许用户在最终合并前做非常小的人工对齐，降低 raw 图局部漂移造成的边缘不贴合问题。

## 接缝亮边修正

- 用户随后反馈：合并后灰色区域周围仍会有一圈偏亮的接缝。
- 已在 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\apply_protection_overlay.py` 中补充“接缝带局部柔化”：
  - 新参数 `--seam-blur`
    - 只对灰区边界带做局部模糊，不影响灰区内部细节；
  - 新参数 `--seam-width`
    - 控制边界带宽度，默认从 2 到 4 像素量级开始试。
- 该修正用于压掉灰区周围一圈发亮或发硬的过渡边，而不是对整片灰区做模糊。

## 中文界面显示修正

- 用户随后反馈：交互脚本里的中文提示基本显示成问号。
- 原因定位：
  - `cv2.putText` 不支持直接绘制中文；
  - 因此在遮罩编辑器和联通块对齐器里，界面文字会退化成问号或乱码。
- 已修正脚本：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py`
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\align_gray_regions.py`
- 修正方式：
  - 改用 `PIL.ImageDraw` + Windows 系统中文字体绘制界面文字；
  - 优先尝试 `NotoSansSC-VF.ttf`、`simhei.ttf`、`Deng.ttf`、`simsunb.ttf`。
- 已完成 `py_compile` 和 `--help` 校验，脚本可正常启动。

## 2.jpg 启动

- 用户明确要求开始按 `$masked-imagegen-protection` 流程处理 `D:\workspace\P图\2.jpg`。
- 当前观察：
  - `2.jpg` 是纵向构图，主体为右侧古塔，天空占比很大，前景还有水面、树林和远山；
  - 这张图适合继续采用“黑区改天空与外部氛围，灰区只保结构并联动光影”的两分区策略。
- 已确认当前还没有 `2.jpg` 对应的手工遮罩输出文件。
- 当前下一步：
  - 启动 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py`；
  - 让用户先为 `2.jpg` 手画灰色联动区并保存；
  - 保存后继续 built-in `image_gen` 首轮 raw 生成、结果回工作区和联动区回盖。
- 当前项目仍然不是 Git 仓库，因此这一轮同样不涉及提交与推送。

## 2.jpg 改走直接生成

- 用户随后明确说明：`2.jpg` 不需要遮罩，直接生成。
- 因此本轮没有继续灰黑分区和回盖流程，而是直接对原图发起一轮 built-in `image_gen` 编辑。
- 提示词策略：
  - 保留原图纵向构图、机位和主要景别；
  - 尽量保留古塔、远山、树线和水域关系；
  - 只把阴天改成更适合朋友圈的真实晚霞天空，并让整张图只发生自然的傍晚暖光联动；
  - 明确避免加人、加船、加鸟、加字、加新建筑、过度风格化或重 HDR。
- built-in 生成图实际落盘位置：
  - `C:\Users\Administrator\.codex\generated_images\019df361-fa11-7652-a515-d3e7179f27e7\ig_02341af21f66efa00169f8ad4b35d0819195d5b3398f55b721.png`
- 已复制到工作区：
  - `D:\workspace\P图\assets\2-edited-direct-v1.png`
- 风险说明：
  - 由于这一轮没有遮罩，也没有后续保护区回盖，模型对古塔、岸线和周边关系做了明显更强的重绘；
  - 这版更接近“重生成风格图”，不属于之前那条“建筑结构尽量锁定”的稳妥路线。

## 2.jpg 直改天空模板收紧

- 用户进一步要求：
  - 重做一版；
  - 只允许修改天空和整体色调；
  - 不允许修改建筑等固定内容；
  - 并把新的提示词模板沉淀到当前 skill 里。
- 已更新 skill 文件：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\references\built-in-prompt-template.md`
    - 新增“无遮罩直改天空模板”；
    - 新增“收紧无遮罩主体漂移”补充块。
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\SKILL.md`
    - 明确当用户拒绝遮罩但仍要求主体尽量不变时，优先使用该模板。
- 已按项目规则执行校验：
  - `PYTHONUTF8=1 python C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py D:\workspace\P图\.codex\skills\masked-imagegen-protection`
  - 校验结果：`Skill is valid!`
- 随后基于新模板对 `2.jpg` 重新发起 built-in `image_gen`：
  - 约束重点为：
    - 只改天空云层、明暗和色彩；
    - 地面景物只允许非常轻微的暖色环境光联动；
    - 如果无法同时满足，就优先保留古塔、树线、岸线、远山和水面结构不变。
- built-in 生成图实际落盘位置：
  - `C:\Users\Administrator\.codex\generated_images\019df361-fa11-7652-a515-d3e7179f27e7\ig_02341af21f66efa00169f8ae5de894819188ba88448f8ee44e.png`
- 已复制到工作区：
  - `D:\workspace\P图\assets\2-edited-direct-v2-sky-only.png`
- 主观结果判断：
  - 相比 `2-edited-direct-v1.png`，这一版明显更接近原图构图和景物关系；
  - 古塔、树线、水岸和远山基本保住，没有再出现上一版那种大范围重构；
  - 但由于仍然是无遮罩直生成，这一版依旧不能视为像素级锁定，只能算“更收紧、更接近原图”的版本。

## 3.jpg 启动

- 用户随后要求开始处理 `D:\workspace\P图\3.jpg`，并明确这张需要画遮罩。
- 当前观察：
  - `3.jpg` 是仰拍门楼场景，建筑屋檐、门洞、牌匾、对联、浮雕和左侧广告牌都属于需要保留的固定内容；
  - 天空面积较大，适合继续采用“灰区联动、黑区可编辑”的遮罩流程，只把天空区域留给生成。
- 已检查 `D:\workspace\P图\生成式替换样例\manual-mask\`，当前还没有任何 `3-*` 遮罩输出。
- 当前下一步：
  - 启动 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\manual_mask_editor.py`；
  - 让用户先为 `3.jpg` 手画灰色联动区并保存；
  - 保存后继续 built-in `image_gen` 首轮 raw 生成、结果回工作区和联动区回盖。

## 3.jpg 首轮遮罩生成

- 用户完成并保存 `3.jpg` 的手工遮罩：
  - `D:\workspace\P图\生成式替换样例\manual-mask\3-manual-mask.png`
  - `D:\workspace\P图\生成式替换样例\manual-mask\3-manual-api-mask.png`
- 遮罩观察：
  - 当前灰区主要覆盖了中间牌匾、两侧对联和左侧广告牌；
  - 门楼其他石雕、屋檐和大面积墙体没有纳入灰区，因此这轮只能优先锁住文字与标识区域，同时用提示词尽量约束门楼整体少改。
- 已用 `view_image` 加载原图、遮罩图和叠加预览后，基于灰黑遮罩模板发起 built-in `image_gen` 首轮生成。
- 本轮生成意图：
  - 只修改黑色可编辑区，把阴沉天空替换成真实、自然、适合朋友圈照片的暖色傍晚天空；
  - 灰色联动区中的文字、广告牌、对联、牌匾边框和纹理只允许轻微光影联动，不允许改内容；
  - 额外要求整座门楼尽量不要被重绘、不要新增物体、不要整体风格化。
- built-in 生成图实际落盘位置：
  - `C:\Users\Administrator\.codex\generated_images\019df361-fa11-7652-a515-d3e7179f27e7\ig_02341af21f66efa00169f8b2b206a481919d02c5c76a3e03ab.png`
- 已复制 raw 结果到工作区：
  - `D:\workspace\P图\assets\3-edited-v1-raw.png`
- 已执行联动区回盖脚本，输出最终结果：
  - `D:\workspace\P图\assets\3-edited-v1.png`
- 本轮后处理参数：
  - `--match-tones`
  - `--match-threshold 32`
  - `--match-erode 4`
  - `--feather 1.0`
- 本轮色调匹配统计：
  - `matched_pixels=95351`
  - `components=6`
- 主观结果判断：
  - 天空替换已经完成，牌匾、对联和左侧广告牌基本守住；
  - 但因为灰区覆盖范围偏小，门楼其他未被遮住的区域仍有轻微重绘痕迹；
  - 这版可以作为第一版结果，但如果用户要进一步压低建筑漂移，下一步应扩大灰区，把更多屋檐和墙体边界纳入联动区后重跑。

## Skill 命令链补全

- 用户随后指出：只要 skill 流程里出现“画灰色区”，就应该一次性给出 3 条命令，而不是只给后两条。
- 已更新 `D:\workspace\P图\.codex\skills\masked-imagegen-protection\SKILL.md`：
  - 新增“画灰色区时必须给出的 3 条命令”小节；
  - 明确命令 1 为手工绘制灰黑分区；
  - 明确命令 2 为联通块手动对齐；
  - 明确命令 3 为读取 `*-region-transforms.json` 后执行回盖，并默认带上 `--region-transforms`；
  - 在最小执行清单中补充：只要这轮要画灰色区，就默认给这 3 条命令。

## 自动预对齐补充

- 用户进一步要求：在进入手动对齐前，先自动对齐一次。
- 新规则：
  - 每个灰区联通块先在上下左右各 `20` 像素范围内做双重循环搜索；
  - 以该联通块区域内“原图平移后与 raw 生成图对应像素的绝对差之和最小”作为最佳初始位置；
  - 自动对齐完成后，立即保存一版自动位移参数、自动对齐预览，以及一张自动回盖图；
  - 如果自动结果还不够准，再进入手动微调并保存最终位移参数。
- 已更新文件：
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\align_gray_regions.py`
    - 启动时自动预对齐；
    - `r` / `0` 重置为“自动对齐结果”而不是全零；
    - 新增 `--auto-range`、`--auto-out`、`--match-tones`、`--match-erode`、`--feather`、`--seam-blur`、`--seam-width`；
    - 自动输出一版回盖图，供用户先看自动结果。
  - `D:\workspace\P图\.codex\skills\masked-imagegen-protection\SKILL.md`
    - 同步补充自动预对齐规则与新命令示例。
- 校验：
  - `python D:\workspace\P图\.codex\skills\masked-imagegen-protection\scripts\align_gray_regions.py --help`
  - `python -m py_compile ...`
  - `PYTHONUTF8=1 python C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py D:\workspace\P图\.codex\skills\masked-imagegen-protection`
  - 结果均通过。
